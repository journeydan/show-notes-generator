Review and fix the handlePullFromCraft function in src/App.jsx. The function reads a Craft document and parses the markdown back into the episode form. There are known parsing bugs:

1. Craft returns URLs in markdown link format: `  🔗 [https://example.com](https://example.com)` — but the parser tries to match bare URLs. Fix the URL extraction regex to handle both formats.
2. The `blocks get --format markdown` response wraps content in `<page>` / `<pageTitle>` / `<content>` HTML-like tags. The parser should strip those before splitting on markdown headers.
3. Line splitting should trim whitespace and filter empty lines.
4. The summary should be the first non-header, non-URL, non-tag text line in each block, not the last.

Read the full function, then make targeted fixes. After fixing, run `npm run build` to verify. Show what changed and confirm the build passes.
